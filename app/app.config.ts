export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      neutral: 'neutral',
    },
    button: {
      defaultVariants: {
        color: 'primary',
      },
    },
  },
})
